import styled from '@emotion/styled'

export const MainContent = styled.div`
  padding: 1rem 2rem;
  width: 100vw;
  display: flex;
  position: fixed;
  margin: 0 auto;
  z-index: 1000;
  justify-content: space-between;
`
